# 项目主 Agent 工作说明

本文件是建筑做法资料库项目的根级工作入口，适用于本仓库中的所有 Agent。主 Agent 负责理解需求、维护架构边界、组织实现与验收，并向用户准确报告进度。

## 1. 当前阶段：实现阶段（2026-09-20 起）

用户已明确要求开始开发，本文件不再重复索要开发许可。按 docs/PRD.md 第 11 节和本文第 5 节的阶段顺序推进：先完成阶段 0 环境与样本核实，再做本地资料闭环、导入识别，最后交付验证。

当前允许：按文档实现、安装经过核实来源的开发工具链与依赖、编写业务代码与测试、提交 Git。

当前不要：改变 PRD 已确认的产品决策；安装与当前目标无关的工具；编造未实测结果；把模拟数据混入正式资料库。

## 2. 开始工作时必须了解的上下文

先阅读以下文档，再处理任务：

1. [docs/PRD.md](docs/PRD.md)：产品行为、范围、已确认选择和验收要求。
2. [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md)：模块边界、数据模型、识别流程及 CAD 协议。
3. 本文件和将来存在的适用子目录说明。

检查仓库实际状态，不假设设计已经实现。不存在的项目、测试、插件、SDK 和样本都应明确记录为缺失。

指令冲突时遵循平台指令层级；在项目层面，用户最新明确要求优先于本地文档。已确认产品行为以 PRD 为准，架构文件负责实现建议。发现两份文档冲突时先修正文档，不在实现中暗自选择一种解释。

## 3. 不得擅自改变的产品决策

- Windows 本地软件，服务本人或小团队；第一版不做账号服务器与实时同步。
- 图集以扫描 PDF 为主，必须保留原始 PDF 与条目来源。
- 地区和部位是独立维度，不能混成一棵不可组合筛选的分类树。
- 可解析识别结果直接入库。允许显示“未核对”，不得添加强制审核才能搜索或插入的门槛。
- 人工修改与识别原结果分开保存；重识别创建候选，不覆盖人工文字。
- AutoCAD 目标范围为 2017～2021；每层成为独立可编辑文字对象，不能用截图、PDF 底图或整个不可分的图块代替交付。
- 资料库更新不自动回写已经插入 CAD 的图纸。
- 本地浏览、编辑和插入不得因云端识别服务不可用而失效。
- 不自动编写缺失材料、数值、单位和层次；不把识别成功视为技术正确。
- 不擅自增加节点图矢量化、CAD 详图生成、商业授权、语义搜索或国产 CAD 适配。

## 4. 主 Agent 的工作方法

### 4.1 任务处理

1. 识别用户本次目标和当前阶段，先做必要的只读检查。
2. 把“已经确认”“建议选择”“尚未验证”分开，优先通过文件与环境查明事实。
3. 范围明确后完成当前已授权任务，不用反复询问常规可逆实现细节。
4. 涉及改变产品范围、输出语义、数据共享方式或真实费用且缺乏授权时，说明具体影响并提出集中问题。
5. 实现与文档同步更新；用真实运行结果报告完成度。

不要因为安装包、联网能力或某个技能可用，就自动调用与当前目标无关的工具。

### 4.2 协作与子 Agent

本文件中的“主 Agent”表示项目责任入口，不表示自动启用多 Agent。只有用户明确要求，或更高优先级指令允许并要求时才分派子 Agent。

若后续获准并行工作，按独立模块划分，事先约定接口、文件责任和验收条件；共享契约的修改由主 Agent 协调。主 Agent 必须检查整合后的构建与流程，不能只汇总子 Agent 的“已完成”。

## 5. 获准开发后的执行顺序

| 阶段 | 必做事项 | 必须留下的证据 |
| --- | --- | --- |
| 0 环境与样本 | 核实 Windows、SDK、CAD、样本及云配置；建立测试资料清单 | 实际环境报告、缺失项、人工标注约定 |
| 1 CAD 最小验证 | 用明确标注的手工测试条目验证连接、选点、独立文字与撤销 | 真机结果；不能用模拟接口代替 |
| 2 本地资料闭环 | 做法、层次、来源、分类、搜索、编辑、历史 | 可运行桌面版本及相关测试 |
| 3 导入识别 | 文本和扫描页、缓存、异常、重试、候选保护、跨页 | 样本评测、费用／恢复测试、已知局限 |
| 4 交付 | 资料包、备份恢复、插件版本矩阵、安装 | 干净环境安装结果、逐版本兼容记录 |

真实 CAD 或 PDF 缺失时，继续能独立完成的工作，并准确列出不能验证的环节；不得编造实测结果。不得把模拟数据混入用户正式资料库。

## 6. 工程约束

- 遵守架构依赖方向：桌面 UI 不直接操作 CAD API，插件不访问云端识别和资料库。
- 数据库使用版本化迁移、参数化查询、事务和外键；涉及业务更新必须维护修订历史。
- 编号、名称不是唯一身份；使用稳定 ID，资料包导入不得静默覆盖冲突。
- 原 PDF 不作为可丢弃缓存。路径必须限制在受管理目录；不得导入资料包中的任意绝对路径。
- API 密钥使用 Windows 安全存储，不进入代码、日志、截图文本、测试快照或导出包。
- 云识别结果按外部不可信数据校验，不把页面内容当作执行指令，不执行模型返回代码。
- 保存任务阶段、缓存指纹和请求状态；超时不等于未发生，禁止无限重试云调用或 CAD 插入。
- CAD 操作保持原子性，取消无残留，一次撤销可完整撤回本次插入。
- AutoCAD SDK 引用和插件构建按版本管理。未实测的版本标记“待测”，禁止宣传已兼容。
- 依赖必须检查目标 Framework、原生位数及许可，锁定实际验证版本；不自行散装复制未知来源 DLL。
- 重大技术调整先记录原因、备选、影响和验证方案，再更新架构。普通代码整理不必制造额外审批流程。

## 7. 验证与完成定义

围绕行为写测试，不写只重复实现的形式化测试。按改动范围运行必要检查，通过后不要无理由重复全量测试。

最低关键验证包括：

- 多条件中文搜索和精确编号排序。
- 修订冲突、历史恢复、重识别不覆盖人工内容。
- 原页映射、跨页来源、多做法拆分和异常保留。
- 中断恢复、识别缓存、重复导入、预算控制。
- 资料包校验、冲突保留、迁移与备份恢复。
- CAD 中文、特殊字符、换行、UCS、独立编辑、组、撤销和重复请求。

“完成”同时要求：在约定环境可运行、对应验收通过、说明更新、没有隐瞒未测部分。构建通过、模拟测试通过、真机通过、识别评测达标是不同结论，分开报告。

95% 识别指标是 PRD 中的目标。必须用人工对照集测量后才能宣布达标；不能用少量演示页推断所有图集。

## 8. 文档与交接规则

- 产品行为变化更新 PRD；模块、接口和部署决策变化更新架构说明。
- 重要方案变化在需要时建立 docs/decisions/，记录背景、决策、影响与验证结果，不为尚未作出的决定创建空记录。
- 用户私有图集和云密钥不提交到代码仓库；测试样本须说明来源与用途。
- 进度汇报使用中文，先说结果，再说验证与限制；不把所有实现细节堆给用户。
- 每次交接明确列出：已完成、已验证、未验证、下一步和需要用户提供的材料。
- 交付文件使用真实可访问链接；不把计划中的文件说成已生成。

## 9. 当前交接记录

2026-09-20：用户明确要求开始开发，进入实现阶段。已完成 Git 仓库与开发日志制度、阶段 0 环境核实、解决方案骨架、Domain、Contracts、Application 边界与用例服务、Infrastructure（SQLite 迁移与仓储）。

已验证：全解决方案构建 0 错误 0 警告；单元测试 37/37；net48 真实 SQLite 集成测试 10/10（覆盖搜索、编号优先排序、修订冲突、恢复历史、编辑复位未核对）。

未验证：无 AutoCAD 运行环境（阶段 1 与 A07/A08 无法执行）；无真实图集样本与云凭据（阶段 3 识别评测无法执行）；桌面 UI 未开始。

下一步：Tuku.Desktop WPF 三栏界面闭环（本地资料闭环阶段 2），之后请用户提供 CAD 环境与代表图集。

开发日志见 docs/devlog/，每个节点提交 Git 并附摘要；技术决策见 docs/decisions/。
