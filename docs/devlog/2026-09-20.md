# 2026-09-20 开发日志

> 阶段：实现阶段第 0 天。任务：初始化仓库与工具链、环境核实。

## 001 · Git 仓库初始化与文档基线

- 节点：建立 Git 仓库，提交既有文档基线
- 提交：见 git log（docs: 项目文档基线…）
- 目标：让项目进入可追溯的版本控制，后续每个节点单独提交
- 变更：
  - `git init -b main`，配置本地 user.name/user.email
  - 新增 `.gitignore`（.NET 构建输出、本地资料库目录 Library/、PDF、密钥等不入库）
  - 提交 `AGENTS.md`、`docs/PRD.md`、`docs/ARCHITECTURE.md`
- 验证：`git log --oneline` 显示基线提交；`git status` 干净
- 摘要：仓库具备版本控制与忽略规则，文档基线已固化
- 问题与追溯：winget 首次安装 .NET SDK 失败（msstore 源网络不可达），已改用 `--source winget` 重试，记录于节点 003

## 002 · AGENTS.md 阶段状态更新

- 节点：按用户“开始开发”的明确授权，更新阶段状态
- 提交：待提交（与本文件同批）
- 目标：AGENTS.md 反映实现阶段授权，保留变更日期
- 变更：
  - `AGENTS.md` 第 1 节改为“实现阶段（2026-09-20 起）”
  - 第 9 节交接记录更新为当前状态
  - 仓库中存在同名小写副本 `agents.md`，内容一致，后续同步维护
- 验证：重新读取磁盘文件确认内容生效
- 摘要：阶段状态与交接记录已同步，开发许可不再重复索要
- 问题与追溯：无

## 003 · 安装 .NET SDK 8.0.425

- 节点：安装开发工具链
- 提交：随节点 005 一并提交
- 目标：获得可构建 netstandard2.0 / net48 / WPF 的 SDK
- 变更：winget 安装 `Microsoft.DotNet.SDK.8`（官方源，安装程序哈希已验证）
- 验证：`dotnet --list-sdks` → 8.0.425 [C:\Program Files\dotnet\sdk]
- 摘要：.NET SDK 8.0.425 安装成功
- 问题与追溯：首次 `winget install` 因 msstore 源不可达（WinHttpSendRequest 12029）失败；改用 `--source winget` 重试成功。定位：winget 默认源包含 msstore 且网络不可达；修复：限定源。无遗留影响。

## 004 · 阶段 0 环境核实报告

- 节点：按 AGENTS.md 第 5 节完成环境与缺失项核实
- 提交：随节点 005 一并提交
- 目标：记录真实环境、缺失项与影响，不假设设计已实现
- 变更：新增 `docs/environment-2026-09-20.md`
- 验证：实测项包括 Windows 11 专业版 26200、.NET Framework 4.8.09221（Release 533509）、Git、网络；确认无 AutoCAD 主程序、无真实图集 PDF、无云端凭据、无 Framework 目标包
- 摘要：工具链已具备；CAD 真机验证与识别评测因缺环境暂缺，已如实记录并请用户后续提供
- 问题与追溯：无

## 005 · 解决方案骨架与构建验证

- 节点：按架构第 7 节建立工程组织并验证可构建
- 提交：本节点
- 目标：解决方案可构建、测试管线可用；net48 目标经 SDK 8 实测
- 变更：
  - `Tuku.sln` 与项目：Tuku.Domain、Tuku.Contracts、Tuku.Application（netstandard2.0）；Tuku.Infrastructure、Tuku.Desktop（net48 WPF）；tests/Tuku.UnitTests（net8.0）、tests/Tuku.IntegrationTests（net48）
  - 引用方向：Desktop→Application；Application→Domain+Contracts；Infrastructure→Application+Domain+Contracts；测试项目按层引用
  - net48 项目设置 `LangVersion=latest`；`App.xaml.cs` 中 `Application` 全限定为 `System.Windows.Application`
  - 新增 `tests/Tuku.UnitTests/SmokeTests.cs`
- 验证：`dotnet build Tuku.sln` 成功（0 警告 0 错误）；`dotnet test tests/Tuku.UnitTests` 1/1 通过
- 摘要：骨架可构建可测试，net48 + WPF + SDK 8 组合实测可行
- 问题与追溯：
  1. WPF 模板不支持 `-f net48`（退出码 127）→ 默认创建后手工改 TFM 解决
  2. classlib/xunit 模板在 SDK 8.0.425 不接受 `-f net48` → 同上处理
  3. net48 默认 C# 7.3 导致 CS8630（nullable）→ 加 `LangVersion=latest`
  4. `App : Application` 与 `Tuku.Application` 命名空间冲突（CS0118）→ 全限定修复

## 006 · Tuku.Domain 领域模型与校验

- 节点：按架构 3.1 实现核心领域模型与业务规则
- 提交：本节点
- 目标：做法、层次、来源、修订、分类、搜索归一化规则进入可测试的领域层
- 变更：
  - `Common/ValidationResult`：校验结果（不抛异常的业务校验）
  - `ValueObjects/PageRegion`：归一化 [0,1] 区域、区域来源与可靠性校验
  - `Entities/Atlas`、`AtlasPage`、`Practice`、`PracticeLayer`、`PracticeSource`、`PracticeRevision`
  - `Entities/PracticeSnapshot`：完整业务快照（恢复历史用）
  - `Taxonomy/TaxonomyNode`、`Tag`：地区／部位独立维度与标签
  - `Search/TextNormalizer`：全角转半角、大小写折叠、空白归一
  - `Search/PracticeSearchFields`：可重建搜索文档字段组合
  - 测试：`tests/Tuku.UnitTests/Domain/` 共 21 个用例
- 关键规则落实：
  - 手动录入允许空编号，但必须有名称和至少一层可插入正文
  - 图集做法必须关联原 PDF 页来源；纯自定义条目无来源
  - 每次保存携带预期修订号，不匹配报冲突
  - 修改正文、恢复历史、采用候选均生成新修订并复位“未核对”
  - 恢复历史不改写历史记录本身（快照只读使用）
- 验证：`dotnet test tests/Tuku.UnitTests` → 21/21 通过
- 摘要：领域规则层完成并有行为测试覆盖
- 问题与追溯：`ValidationResult.cs` 初版漏 `using System.Collections.Generic`（CS0246）→ 本节点内修复并回归通过

## 007 · Tuku.Contracts 识别与 CAD 协议契约

- 节点：按架构 4.1 / 5.3 定义识别输入输出与 CAD 消息契约
- 提交：本节点
- 目标：供应商响应与 CAD 通信有稳定数据契约，可独立测试
- 变更：
  - `Recognition/`：RecognitionTask、RecognitionOutcome（成功／失败／结果未知三态，超时不等于成功）、RecognitionRawResponse（原始响应按外部不可信数据保存）、ExtractedPracticeCandidate、PageExtractionResult（区分“无做法内容”与“需处理”）
  - `Cad/CadProtocol`：协议版本 1、消息类型常量、大小上限、版本判定
  - `Cad/CadMessages`：Hello/Capabilities、ListDocuments、InsertPractice（请求 ID 幂等）、InsertStatus 七态、GetRequestStatus、CancelRequest、端点登记
  - `ProtocolMessage`：长度前缀 JSON 信封（版本、请求 ID、类型、JObject 负载）
  - 依赖：Newtonsoft.Json 13.0.3（MIT，netstandard2.0 与 net4x 通用）
  - 测试：`tests/Tuku.UnitTests/Contracts/` 7 个用例
- 验证：`dotnet test tests/Tuku.UnitTests` → 28/28 通过
- 摘要：契约层完成；MText 控制符（如 `\P`）在契约层按普通文字原样传递，转义责任在 CAD 插件侧
- 问题与追溯：无

## 008 · Tuku.Application 边界接口、DTO 与搜索排序

- 节点：按架构 4.1 定义应用层边界与查询模型
- 提交：本节点
- 目标：Infrastructure 有可实现接口，Desktop 有可用查询模型；排序规则可单测
- 变更：
  - `Abstractions/RepositoryInterfaces`：IPracticeRepository（查询/保存/恢复/核对）、IAtlasRepository、ITaxonomyRepository、IPdfSource、IRecognitionProvider、IPracticeExtractor
  - `Abstractions/ILibraryPackageService`：资料包导出/预览/导入边界与 manifest 结构
  - `Dtos/PracticeDtos`：查询条件、列表项、详情、编辑命令（携带预期修订号）、保存结果（已保存/修订冲突/校验失败）
  - `Search/SearchRanker`：编号精确匹配(3)＞名称(2)＞正文等(1)；多关键词全部命中才返回
  - 测试：SearchRanker 4 个用例
  - Domain 增加 `InternalsVisibleTo("Tuku.UnitTests")` 供搜索字段构造测试
- 验证：`dotnet test tests/Tuku.UnitTests` → 32/32 通过；`dotnet build Tuku.sln` 0 错误 0 警告
- 摘要：边界与查询模型就位，排序优先级规则落实并有测试
- 问题与追溯：
  1. `PagedResult.TotalPages` 误用小写 `pageSize`（CS0103）→ 改 `PageSize`
  2. `PracticeSearchFields` 私有构造导致测试无法构造 → 改 internal + InternalsVisibleTo
  3. 空 JObject 反序列化返回实例而非 null → 修正断言语义
  4. 测试项目可空警告（CS8602/8625/8604/8618）→ 以 null 检查助手与可空注解消除，全构建零警告



